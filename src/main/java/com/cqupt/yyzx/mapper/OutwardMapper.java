package com.cqupt.yyzx.mapper;


import com.baomidou.mybatisplus.core.mapper.BaseMapper;

public interface OutwardMapper extends BaseMapper<OutwardMapper> {
    // 这里可以添加自定义的查询方法
    // 例如：List<Outward> findBySomeCriteria(String criteria);
}
